server:
  port: 8087
spring:
  datasource:
    url: jdbc:h2:mem:fulfillmentdb
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
